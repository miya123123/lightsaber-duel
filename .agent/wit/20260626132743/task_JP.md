# Task_JP

## ユーザープロンプト

mainとorigin/mainのブランチを統一してください。

## 完了条件

- `git pull --ff-only` を実行しないこと。
- 可能な限りローカル側とリモート側の作業を両方保持すること。
- ローカル `main` に `origin/main` の内容を取り込むこと。
- 明示的な承認なしに GitHub へ push しないこと。
- 指定の結果報告書と作業ログを作成すること。
- `git add .` と `git commit` を実行すること。

## 状態

ローカル作業として完了しました。リモートへの push は承認待ちです。
